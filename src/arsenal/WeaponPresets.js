// Curated starting points — one per base type — so each silhouette has a strong
// default to showcase. The workbench rolls a full config from {seed, type} on select,
// then the user can tweak any slider.

export const WEAPON_PRESETS = Object.freeze([
  { name: "Spark Sidearm", seed: "ember-9", type: "sidearm" },
  { name: "Coil Carbine", seed: "voltage-3", type: "longarm" },
  { name: "Prism Rail", seed: "monolith-7", type: "heavy" },
  { name: "Gravity Fork", seed: "halo-12", type: "exotic" },
]);
